# Runtime dependencies for the optional TCIA query MCP/REST server.
#
# The core TCIA skill scripts use only the Python standard library where
# possible. Install this file only on hosts that will run the server.
mcp==2.0.0
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
