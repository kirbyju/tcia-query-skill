interface:
  display_name: "TCIA Query Skill"
  short_description: "Query TCIA snapshot metadata across CRDC systems"
  default_prompt: "Use $tcia-query-skill to find TCIA-published datasets from the SQLite snapshot or release exports, prefer IDC/idc-index for public DICOM details, and explain access routes, citations, viewer links, and download or manifest options."

policy:
  allow_implicit_invocation: true
