interface:
  display_name: "TCIA Query Skill"
  short_description: "Query TCIA datasets and verified TCIA-based publications"
  default_prompt: "Use $tcia-query-skill through MCP or REST when available to verify TCIA datasets or publications with provenance, access terms, citations, and official routes."

policy:
  allow_implicit_invocation: true
